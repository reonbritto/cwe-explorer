apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cwe-data-pvc
  namespace: puresecure
  labels:
    app: cwe-explorer
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 2Gi
