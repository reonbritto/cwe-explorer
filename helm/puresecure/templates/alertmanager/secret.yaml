{{- /*
  The alertmanager-smtp-secret is created and managed by External Secrets Operator.
  See templates/secrets/external-secret.yaml — the ExternalSecret named
  "alertmanager-smtp-secret" writes the ALERTMANAGER_SMTP_PASSWORD key from
  Azure Key Vault into a Kubernetes Secret of the same name.

  This file intentionally contains no manifest; it exists only as documentation.
*/ -}}
